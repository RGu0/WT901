# 变更记录

本文件记录对**公开契约**（`wt901` 命名空间里的名字）的改动。内部模块路径不构成
契约，其移动不在此记录。

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 未发布

### 破坏性变更

* **`Telemetry.read_version()` 在两个版本寄存器都为零时抛
  `UnexpectedRegisterResponse`**，不再走遗留分支返回 `"0"`。

  `"0"` 是一个看着像版本号的假值。选择抛异常而不是附标志：版本号只有「显示与比对」
  一个用途，不可信就毫无用处，与 `read_mac` 同理；经 `read_device_info` 的逐项容错后
  `DeviceInfo.version` 自然落成 `None`（RAY-305）。

* **`ChipTime.__str__` 在字段不构成合法日期时不再打成时间戳样式。** 全零回读此前会
  打出 `2000-00-00 00:00:00.000` —— 看着像个时间戳，而它不是。

### 修复

* **`note` 里含 U+2028 / U+2029 时，录制文件读不回来。**

  头部用 `ensure_ascii=False` 写出，这两个字符原样落进文件；而读取用
  `str.splitlines()` 切行，它在这两个字符上断行，于是头部被劈成两半、后半段又被
  当成数据行。JSON Lines 的行分隔符只有换行，`splitlines()` 认的那一串（`\r`、
  `\x0b`、`\x0c`、`\x1c`–`\x1e`、`\x85`、U+2028、U+2029）比它宽。

  改成按文件行读之后不会了。这是 `open_recording` 重构顺带修掉的，不是它的目标，
  但同样有测试钉住（RAY-295）。


* **一次 `0x71` 应答携带 8 个寄存器，本库此前按 4 个解，多出的一半被静默丢弃。**

  `RegisterResponse.values` 因此从 4 个元素变成 8 个。取前几个的调用方不受影响；
  `value_at()` 的覆盖范围翻倍（读 `0x3A` 现在也能取到 `0x40` 温度），越界行为不变。
  若有调用方在断言 `len(values) == 4`，那个断言现在会失败——它断言的本来就是一个
  错误的事实。

  连带 `Telemetry.read_serial_number()` **少跑一趟 BLE 往返**：序列号占 6 个寄存器，
  过去因为「一次只回 4 个」要读 `0x7F` 与 `0x82` 两次，现在一次读全。寄存器读与
  `0x61` 实时流抢同一条链路，省下的是采样带宽。返回值不变。

  帧长本来就算得出来：负载 18 字节 = 2 字节起始地址 + 16 字节寄存器区 = 8 个寄存器，
  按 4 解会有 8 个字节没有说法，而手册摘要 §3.3 也一直写着 8 个。**这不是上游资料
  缺陷，是本库自己读错了**，而且离线测试永远发现不了它：构造帧的测试工具与被解码的
  代码用的是同一个错误常量，两边一起错就一起「通过」。

  真机核实用的是**语义可自证**的量（`tools/probe_register_width.py`，2026-08-27 一台
  WT901BLE67，证据 `ray-292/register-response-width/acceptance/`）：读 `0x2E`，第 5–6
  个位置解出的分/秒与毫秒夹在前后两次直读 `0x30` 的结果之间；读 `0x3A`，第 7 个位置
  是 30.04 °C 而直读 `0x40` 为 30.03 °C。两者都落在原先被丢弃的那 8 个字节里，各 3 轮
  全部成立。真机原始负载已作为 fixture 进 `tests/test_register_response_width.py`
  （RAY-292）。

### 更正

* **`Bandwidth` 此前声称「只登记已核实的两档」，那句话不成立——三档的标称值一个都没
  被实测过。**

  `ray-171` 的验收清单本就记着「`BANDWIDTH` 未逐档探测」；早期证据里那句「带宽已设为
  20 Hz，读回 `0x04`」只是把编码写进去再读回来，与截止频率是多少无关。`HZ_20` 与
  `HZ_256` 随 RAY-168 从官方 SDK 抄入，数字来自维特通用编码表。

  这不是措辞洁癖：本库把「已核实」当成一个有分量的词用——`ReturnRate` 排除 `0x0A` 靠
  的就是它（通用表标 125 Hz，实测 99.29 Hz）。这个词用错地方，会让读者以为带宽档位
  也经过同等强度的验证。docstring、`docs/protocol.md` 与拒绝信息的措辞一并改正，并
  新增一条测试挡住它退回去。

  **拒绝信息的文本变了**：不再说「未在真机上核实 / 当前已核实」，改为「未登记 / 当前
  登记」。异常类型仍是 `UnsupportedRegisterError`，按类型捕获的调用方不受影响；按文本
  匹配的会受影响。

  真机上确立的事实（2026-08-27，一台 WT901BLE67，`tools/probe_bandwidth.py`，证据
  `ray-298/bandwidth-42hz/acceptance/`）只有两件：设备接受三个编码并照常出数；三档
  截止频率单调有序（20 Hz 处 `0x03` 衰减 −1.4 dB 而 `0x04` 为 −7.0 dB，50 Hz 处
  −2.3 dB vs −13.1 dB）。用噪声底功率谱比值量截止频率的那次尝试**自校验没过**——标称
  20 Hz 的 `0x04` 测出 10.9 Hz，因此同一次测出的 `0x03` = 35.6 Hz 同样不算数。经过与
  局限记在 `docs/protocol.md` §6.2（RAY-298）。

### 新增

* **`Bandwidth` 补齐上游表的全部七档**：新增 `HZ_188`(`0x01`)、`HZ_98`(`0x02`)、
  `HZ_10`(`0x05`)、`HZ_5`(`0x06`)，与原有的 `HZ_256`/`HZ_42`/`HZ_20` 一起覆盖
  `0x1F` 的 `0x00`–`0x06`。`0x07` 及以上仍然拒绝。

  在此之前只登记三档，需要 98 Hz 的调用方只能用通用 `write(0x1F, 0x02)` 绕过——
  那会同时绕开「未登记编码一律拒绝」这道白名单，比登记它更糟。

  **七个标称值全部来自维特通用编码表，本库一档都没实测过**，新增的四档连「设备
  接受它」都没验证过。成员名在这里是**转述**（「上游表把这个编码标为 98 Hz」）而
  非断言（`ReturnRate.HZ_100` 意思是「本库量到过 100 Hz」）——同一张通用表在速率上
  被证伪过一次（`0x0A` 标 125 Hz、实测 99.29 Hz），没有理由假定它在带宽上都对。
  要拿这些数字做采样率/抗混叠的设计决策，先自己测。详见 `docs/protocol.md` §6.2
  与 `Bandwidth` 文档（RAY-304）。

* **`open_recording()` / `RecordingReader` / `RecordingHeader`** —— 录制文件的流式
  读取入口，峰值内存与文件大小无关。

  ```python
  with open_recording(path) as reader:
      print(reader.header.device_id)   # 头部在打开时就有，不必读完整份文件
      for chunk in reader:             # 逐行产出
          ...
  ```

  `read_recording` 的行为与签名不变，**并且现在建立在同一段解析代码之上**——两条
  路径对末行截断与中间行损坏的判定因此不可能分叉，这是结构上的保证，不是靠测试
  比对出来的。

  `RecordingReader.truncated` 是 `bool | None`：**迭代完成之前是 `None`**。流式读取
  在读到文件尾之前无从知道末行完不完整，给 `False` 就是把「还不知道」说成「没截断」
  ——与 `SerialNumber.value`、`ChipTime.is_plausible` 同一条线。整份读完之后构造出来
  的 `Recording.truncated` 仍然是 `bool`。

  **不是尾随读取**：它读的是一份已经写完的文件。跟着写入方一起读涉及「读到一半的行
  下次要重读」这类语义，本入口不提供。

  `tools/record_session.py` 已改用它——那里只需要段数、字节数与时长三个统计量，单趟
  扫描就够（RAY-295）。


* **`Bandwidth.HZ_42`（`0x1F` = `0x03`）。**

  《BS-BT91 硬件适配与时间同步方案 v0.1》§3.1 七步序列的第 ③ 步要的就是这一档。在此
  之前它会被 `set_bandwidth()` 拒绝，下游只能用通用 `write(0x1F, 0x03)` 绕过——而绕过
  正是 RAY-241 / RAY-291 反复论证要避免的反模式，且会绕开「未登记编码一律拒绝」这道
  防线。七步序列现在可以被具名 API 完整表达。

  未列出的编码（`0x01`=188、`0x02`=98、`0x05`=10、`0x06`=5 Hz）仍然拒绝：拒绝防的是
  **误**写，与证据强度无关。

* **`ChipTime.is_plausible` 与 `Quaternion.is_plausible`** —— 寄存器整块回读全零是
  本器件有据可查的现象（`docs/protocol.md` §10），这两处此前会交出看着正常的假值：
  `2000-00-00`（日历上没有第 0 月第 0 日）与 `(0,0,0,0)`（模为 0，不表示任何朝向）。

  **这两处附标志而不抛异常，理由不同于 `read_version`：**

  - `ChipTime`：本方法要回答的正是「设备是不是刚上电」。一台时钟从未设过的设备
    **可能本来就报全零**，而本库没有核实过它上电时报什么。抛异常会把「时钟未设」
    和「这次没读出内容」一起抹掉，而前者正是调用方问的那件事。
  - `Quaternion`：原始值是判定成因所需。**归一化前先看这个属性** —— 对模为 0 的
    四元数归一化得到 NaN，它会一路飘进姿态解算。

  判据都只看「原始值是否全零」/「月日是否可能存在」，不划容差线、不检查模是否接近
  1：器件给的是定点数，正常读数的模也只是接近 1，划线就是发明没实测过的规则。

  `read_temperature()` **有意不在此列**：0 °C 是可能的真实测量。本库只挡不可能的值。

  `TelemetryPoller` 的行为随之明确：不可信的读数照常写进属性，可信与否随值一起走，
  不做「保持上一次的有效值」—— 那会让陈旧值冒充当前状态，比一个自带标志的新值更难查。

## 0.2.0 — 2026-08-27

### 升级须知

**`Telemetry.read_serial_number()` 的返回类型变了**，从 `str` 变成 `SerialNumber`。
调用方要改用 `.value`（`str | None`）与 `.is_plausible`，理由见下方「破坏性变更」。

本仓库之外唯一已知的调用方是 gait-IMU，升级前核查过：它**零处使用**该 API，因此
这次升级对它是安全的。把这条写在这里，是为了让下一个读到「破坏性变更」四个字的人
不必自己去查影响面——**运气好不等于没风险**：假如它当初用了序列号，这个版本就升不动。

其余改动都是新增或行为收紧，没有需要调用方改代码的地方。

### 修复

* **一条流长期静默时，`merge()` 不再把存活流的产出压到 `1 / max_latency`。**

  等待预算原本按流反复支付：每发一个样本都要重新等一遍那条不说话的流，于是存活
  流被钉死在一个与设备速率无关的绝对上限（默认 20 Hz）。真机上双设备 200 Hz 关掉
  一台，存活那台从 198.6 Hz 塌到 19.5 Hz，其余九成样本在设备队列里被静默丢弃
  （RAY-190）。它满足「另一台没有停」的字面要求，交给上层的却是一条十抽一的流。

  现在超出预算的流被移出等待集，直到它自己再开口；这段时间合流按存活流全速推进。
  该缺陷自 0.1.0 起存在，与采样率无关（100 Hz 下同样塌到 20 Hz）。

### 破坏性变更

* **`Telemetry.read_serial_number()` 改为返回 `SerialNumber` 而不是 `str`。**

  全零的回读此前会变成空字符串 `""`，在 `DeviceInfo` 里与「这一项没读到」长得一模
  一样。而这不是假想情形：本器件真机上读到过逐字节全零的序列号（RAY-172 记过一次，
  RAY-279 的取证里两台设备再次全零）。下游拿 `""` 当绑定键会让**每台设备的键都相同**
  ——两台设备安静地互相冒充，正是 RAY-279 反复警告的那种失败（RAY-293）。

  ```python
  serial = await device.telemetry.read_serial_number()
  serial.value        # str | None，全零时为 None
  serial.raw          # bytes，始终给出，判定成因需要它
  serial.is_plausible # 这次读数是否可能是真实内容
  str(serial)         # 可打印：全零时是空串
  ```

  形状与 `Battery` 一致（RAY-182 的 `raw = 0`），而**不是**与 `read_mac` 一致（那里
  全零直接抛异常）。区别是有意的：MAC 只有身份一个用途，不可信的 MAC 毫无用处；
  序列号是设备信息，「它读出来是全零」本身就是诊断线索，值得原样交出来。

  `DeviceInfo.serial_number` 的类型不变（`str | None`），新增 `serial_number_raw`
  区分两种 `None`。非 ASCII 字节以 `\uFFFD` 替换的行为不变——掺乱码与全零是两回事。

### 新增

* **`SerialNumber`** —— 见上，从包根导出。

* **`Mounting`、`Settings.mounting`、`RegisterAccess.set_mounting` / `read_mounting`**
  —— 安装方向（寄存器 `0x23`）现在能具名表达，与 `set_output_rate` / `set_bandwidth` /
  `set_algorithm` 同构。

  此前只能走通用的 `write(0x23, 0)`。编码不像 `0x24` 那样反直觉（0 水平、1 垂直），
  但**写错同样不报错**：装反了只是让姿态解算的重力轴对不上，数据一直偏，而链路、
  速率、丢包这些可观测量全部正常（RAY-291）。

  **即使写的是出厂默认值也不要省掉它。** 下游适配文档给的是七步下发序列，其中这一步
  多半是幂等的——它的价值不在于改变什么，而在于让「一份配置快照完全决定设备状态」
  成立。省掉它就等于依赖设备残留的配置，而模块会被别处用过、配置又固化在 flash。
  这层理由写在 `Register.MOUNTING` 与 `set_mounting` 的文档里，并有测试钉住。

  按**配置型**处理（`remember=True`），进 `applied_writes` 并被 `replay()` 重新下发，
  与 `0x24` 同类。`settings()` 的下发顺序为 速率 → 带宽 → 算法 → 安装方向，与适配
  文档 §3.1 一致并有测试钉住。

* **`read_recording(path, *, tolerate_truncated_tail=False)` 与 `Recording.truncated`**
  —— 崩溃截断的录制文件不再整份不可读。

  进程被 `kill -9`、掉电、或写到一半被打断时，文件的最后一行必然是残行，而严格
  解析会让**此前全部完好的数据一起失效**。真机上一份 30 分钟 200 Hz 的录制截断
  后，前 633 行完好，一行都取不出来（RAY-280）。而采集轮次中途被打断是常态。

  **默认行为不变，仍然严格**：静默容忍会把「这份文件坏了」变成一个没人注意到的
  事实。要容忍就得明写 `tolerate_truncated_tail=True`，拿到结果还要看
  `Recording.truncated` 才知道发生了什么。

  只容忍**最后一行**的 JSON 解析失败。中间行损坏说明文件被改过或拼接过，照旧拒绝
  并指明行号；末行若能解析出 JSON 但时刻倒退、hex 非法，那是损坏而不是截断，两种
  模式都拒绝。这个边界是准确的而不是保守的：数据行 `{"hex":"…","t":…}` 的任何真
  前缀都不是合法 JSON，所以「末行解析不了」恰好等价于「末行被截断」，有测试钉住
  这条性质。头部自身被截断不在容忍范围内——那种文件没有可救的数据。

  `truncated` 描述的是**来源文件**而不是这份数据，因此不进文件格式：把救回来的
  录制写出去得到的是一份完好的文件。`ReplayTransport` 不需要另开开关，救回来的
  `Recording` 直接传给它的构造函数即可。

* **`Telemetry.read_mac()`、`DeviceInfo.mac`、`Register.MAC`** —— 设备自报的蓝牙
  地址（寄存器 `0x66`）现在能具名读到，返回 `XX:XX:XX:XX:XX:XX`。

  此前本库**没有任何一个可跨主机持久化的设备身份**：`DiscoveredDevice.address` 在
  macOS 上是 CoreBluetooth UUID（它自己的文档就写着「不要跨主机持久化」），广播名
  同批次重复（两台设备都叫 `WT901BLE67`），序列号真机上读到过逐字节全零。下游要把
  模块绑到「左脚/右脚」并跨主机保持这个绑定，只能自己发 `FF AA 27 66 00` 并解析
  `0x71` 应答——那是把设备知识推回业务仓库（RAY-279）。

  **字节序是反的**：`0x66`–`0x68` 按小端取出的 6 字节是地址的空口顺序，显示时整体
  倒过来。手册没给这个排布；它由 2026-08-27 两台真机的实测应答推定——四种可能排布
  里只有这一种让两台设备都得到合法的 BLE 随机静态地址。**在 Windows/Linux/Android
  主机上看一眼同一台设备的 MAC 即可证实或推翻**，依据与推理写在 `read_mac` 的文档
  与 `docs/protocol.md` 里。

  回读全零抛 `UnexpectedRegisterResponse` 而不是返回 `00:00:00:00:00:00`：序列号
  寄存器就出现过全零（RAY-172），MAC 若也如此，一个「所有设备都相同」的绑定键会让
  两台设备安静地互相冒充。`read_device_info()` 里这一项照旧逐项容错，读不到为 `None`。

* **`AlgorithmMode`、`Settings.algorithm`、`RegisterAccess.set_algorithm` /
  `read_algorithm`** —— 6 轴/9 轴姿态解算算法（寄存器 `0x24`）现在能具名表达，与
  `set_output_rate` / `set_bandwidth` 同构。

  此前只能走通用的 `write(0x24, 1)`，这把「1 表示 6 轴相对航向」这条设备知识推回给
  每个调用方。**而这个编码与直觉相反：值大的 1 对应的是轴少的那个。** 写反了设备
  不报错，只会让航向从相对航向变成依赖磁力计的绝对航向，在室内金属环境下悄悄劣化
  ——数据里看不出来（RAY-241）。

  按**配置型**处理：进 `applied_writes`，重连后由 `replay()` 重新下发。这与校准写入
  相反（RAY-173：校准是一次性动作，重放会重做零位标定）；算法模式必须重放，否则
  重连后航向语义可能在调用方毫不知情的情况下改变。

  手册注明「Z 轴角度归零」需先切到 `SIX_AXIS` 才生效，该门控关系写在
  `Register.ALGORITHM` 与 `set_algorithm` 的文档里。

* **`MergeStats.emitted_while_stalled`** —— 至少有一条流处于停滞状态时发出的样本
  数。这段输出是单边的：顺序只在存活流之间成立。合流不会因此变慢或报错，所以不看
  这个字段就发现不了。

  `latency_flushes` 的含义随之更干净：一条持续静默的流只贡献一次，不会被刷爆。

### 变更

* **`battery_percent()` 与 `Battery.percent` 现在可能是 `None`。** 原始值是电压
  ×100，非正数不可能是一次真实测量；此前阶梯表把它映射成 0%，也就是把一个无效
  读数说成「没电了」。真机上确实读到过原始值 0（RAY-182）。

  新增 `Battery.is_plausible`。`DeviceInfo` 里两种 `None` 靠 `battery_raw` 区分：
  它为 `None` 说明没读到，它有值而 `battery_percent` 为 `None` 说明读到了但数值
  不可能。

  **阈值以下的合法低电量读数不受影响**：339 仍是 0%，340 仍是 5%。

* **持久化写入（`persist=True`）现在会在 `save` 之后等待 flash 写完再返回**，
  新增 `DEFAULT_SAVE_DELAY`（0.5 秒）与 `RegisterAccess.save_delay`。

  真机实测：设备写 flash 约需 300 ms，期间对部分寄存器回读出中间状态。写事务的
  文档说它是原子的，而在设备仍在忙时就返回，这个承诺不成立。等待在事务锁内，
  并发的读也因此不会挤进这个窗口。

  `persist=False` 不写 flash，不受影响——重连后的配置重放走的正是那条路径。

## 0.1.0

首个版本。WT9011DCL-BT50 的 BLE 接口：设备发现与连接、`0x55` 协议解析、寄存器
读写事务、校准、遥测、多设备并发采集。

### 许可证

MIT。

### 公开 API

* `scan` / `DiscoveredDevice` —— 设备发现
* `WT901Device` —— 生命周期、采样流、背压、自动重连
* `RegisterAccess`（`device.registers`）—— 寄存器读写事务与输出配置
* `Telemetry`（`device.telemetry`）—— 磁场、四元数、温度、电量、芯片时间、序列号、版本号
* `Calibration`（`device.calibration`）—— 加计校准与磁场校准
* `merge` —— 多设备样本流合并
* `wt901.protocol` —— 纯函数协议层（帧解码、指令构造、SI 换算），零 I/O

### 单位约定

对外一律 SI：加速度 m/s²、角速度 rad/s、角度 rad、磁场 µT、温度 °C、位移 m。
**不提供以「度」为单位的并行接口**——器件原始语义是度，转换只在一处发生，需要
原始计数值时走 `.raw`。

### 有意不做的事

* **时钟同步补偿**：`t_host` 是主机接收时刻，含蓝牙栈抖动；补偿是上层职责。
* **位移模式（`0x96`）**：位移帧与实时数据帧共用 Flag `0x61` 且布局无法区分，
  本库拒绝按运动语义解析而不是猜。
* **串口透传**：需要额外适配器硬件，目前无该硬件，不预留看似可用的接口。
* **速率编码 `0x0A`**：通用表标 125 Hz，真机实测 99.29 Hz，测不准的不进枚举。

### 不属于公开契约的部分

`wt901.recording` 与 `wt901.transport.replay` 是**测试设施**，用于在没有硬件的
环境里回放真机字节流。接口不承诺稳定。
