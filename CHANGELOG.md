# 变更记录

本文件记录对**公开契约**（`wt901` 命名空间里的名字）的改动。内部模块路径不构成
契约，其移动不在此记录。

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 未发布

### 升级须知

**`MagneticField` 多了一个你应该去看的判据。** 行为没有破坏性变更，但**此前认为
「`value` 不是 `None` 就能用」的代码，在 6 轴模式下一直在用一个可能任意陈旧的数**。

真机确立（RAY-344，两台 WT901BLE67）：设备处于 6 轴解算（`0x24 = 1`）时**不采样
磁力计**，`0x3A`–`0x3C` 停在一个固定值上不再更新——转动设备、开着实时流、跨连接会话
都不变。**两台设备的出厂状态都是 6 轴**，所以这不是配错才遇得到的边角情形。

更糟的是陈旧的**时间尺度没有已知上界**：两台切回 6 轴后读到的都是切去 9 轴**之前**
的旧值，逐字节相同，中间隔了十几个小时、一整段 9 轴运行和多次断连重连。

```python
field = await device.telemetry.read_magnetic_field()
if field.may_be_stale:        # ← 新增；6 轴下为 True
    ...                       # 数值可能是几小时前的，别当成当前磁场
if field.is_calibrated_unit:  # ← 与上面**互相独立**：说的是单位，不是新鲜度
    use(field.value)
```

### 新增

* **`MagneticField.algorithm_mode`** —— 读数产生时设备的姿态解算算法（寄存器
  `0x24`），未取到时为 `None`。`0` 是 9 轴、`1` 是 6 轴（编码与直觉相反）。

* **`MagneticField.may_be_stale`** —— 这个读数是否可能任意陈旧。**只有确认设备在
  9 轴时才为 `False`**；6 轴为 `True`，`algorithm_mode` 为 `None`（没取到）时**也是
  `True`**——拿不准就不声称它是新鲜的，与 `Battery.percent`、`SerialNumber.value`
  同一条规矩。要区分「确知陈旧」与「不知道」，看 `algorithm_mode` 本身。

* **`Telemetry.algorithm_mode`** —— 已缓存的 `0x24`，与 `magnetic_field_type` 并列。

  **它与 `magnetic_field_type` 的缓存不是一回事**：量纲类型是硬件属性、运行中不变；
  算法模式是**配置**，`set_algorithm()` 随时能改。所以取值时**写入优先**——本库在这
  条连接上写过 `0x24` 的话以写入值为准，这个缓存只在从没写过时用到。

#### 代价与两条局限

`0x24` **只探一次并缓存**，不给每次 `read_magnetic_field()` 增加一次寄存器读（那会与
`0x61` 实时流抢链路）。

* **本库之外改的算法模式看不见**（例如维特上位机改了它）。
* **`0x24` 读不到时新鲜度按未知处理**（`may_be_stale` 为 `True`），且**不再重试**——该
  寄存器不在官方地址表里，某些固件上可能不应答，每次读都重试会白等一整轮读超时。一次
  `set_algorithm()` 就能让它重新变为已知。读失败**不会让磁场读取失败**：数据本身是读到
  了的，为了取不到一个标志而让整次读取失败没有道理。

`TelemetryPoller.magnetic_field` 在 6 轴下**每个周期都会写进一个新对象**，而对象里的
`raw` 可能自上电起就没变过——轮询器只保证「这是刚读回来的」，保证不了「这是刚测出来
的」。类 docstring 已一并更新。

### 更正

* **磁场量纲的三方矛盾仍未定论，但协议文档的 `×0.1` 已被实测排除**（RAY-312 scope 2）。
  **`magnetic_field_to_ut()` 的行为一行没改**，仍按 `0x72` 分档（SDK 那份）；变的是
  `docs/protocol.md` §5.7 里那句「本库没有实测过任何一档」——现在实测过一档了。

  13 次采集、2 台 WT901BLE67、`0x72 = 6`、北京（IGRF 总强度 54.9717 µT，WMMHR-2025）：
  **协议文档主张的 `×0.1` 偏差 91–94%，13/13 次被排除**，跨设备、跨磁环境、无论是否通过
  质量闸。点云确为球面的那台测得 **0.00636 / 0.00655 µT/count**，与 SDK 的 `1/150` 差
  4.7% / 1.7%。

  **但这不构成对 SDK 的判定**，原因是预注册判据在这一档上给不出结果：判定规则要求「唯一
  一个偏差 ≤15% 且其余 >30%」，而 SDK 与 Python 示例**只差 1.25 倍**——即使测准，另一个的
  偏差最多 20–25%，够不到排除线。**测得越准越判不出来。** 要定论需要一个已知且可控的场强
  （Windows + 维特上位机），不是地磁量级。经过与两条采集规程的教训记在 §5.7 与 §10。

* **`tools/probe_mag_scale.py` 的第 0 步可分性预检只比较了三个候选中的一对，已补全**。
  此前只算 `SDK ÷ 协议文档`：type 6 差 15 倍，判「能分开」，方法据此被认为可行；而
  `SDK ÷ Python` 只差 1.25 倍，低于判定规则所需的 1.30 倍（`1 + REJECT_TOLERANCE`），
  **那一对在数学上永远判不出来**。整轮取证做完才发现方法从一开始就不可能给出定论。

  改为三个候选**两两都比**，并按判定规则反推所需的最小倍数。**判定门槛本身一个字未动。**
  现在 `0x72 = 6`（以及 `= 5`）会在采集之前就被拦下。

* **`read_rssi()` 在 macOS 上「可能可得」改为「实测可得」，但同时明确了一条此前
  含混的局限**（RAY-310 scope 2）。行为一行没改，改的是调用方能依赖什么。

  README 的平台表此前写「macOS：可能可得」，`docs/protocol.md` §8.1 末尾写「本机是否
  真能读到，尚未实测」。现按取证前预注册的判据实测过：**60 次读取 60 次成功**，
  全距 −71..−33 dBm，两个条件都满足（判据 1）。一台 WT901BLE67、Darwin 25.6.0、
  `bleak 0.22.3`、CPython 3.11.15，证据 `ray-310/rssi-platform-verdict/acceptance/`。

  **要紧的是它没有证明什么。** §8.1 开头论证「RSSI 是原因侧的量、能在丢包发生之前
  预警」——那是立项时的论证，**这次实测没有验证它**：同一次采集里 `resync_count` 与
  `dropped_samples` 全程为 0，链路在 −71 dBm 上也没降级，因此没有任何 resync 事件
  可以与 RSSI 曲线对照。实测确立的只有两件：这个量读得到；它随信号条件变化。
  把「读得到」当成「能预警」用，会得到一个没有依据的告警阈值。§8.1 与 README 都已
  写明这条界线。

* **`tools/probe_rssi.py` 打印的判读与它自己预注册的判据对不上，已修**。不影响上面
  那次取证的结论（60/60 在任何一版门槛下都成立），但它在边界上会给出相反的判读：

  - 次数门槛写的是 `int(N * 0.9)`，N=60 时得 **54**，而预注册的原文是「≥ 55」。
  - 判定**只看次数**，没有把预注册的第二个条件「值落在 −100..0 dBm」算进去——它只被
    单独打印了一行。读到越界值时仍会打印「判据 1 成立」。

  改为按 `55/60` 的比例取上整，并要求两个条件同时满足；次数够但有值越界时**不判读**
  （那是读数本身可疑，不属于判据 3 说的「读不到」）。**判据本身一个字未改。**

* **`DEFAULT_READ_RETRIES` 的 docstring 从「未实测，沿用经验值」改为「已实测，但实测
  没有给出改动它的依据」**（RAY-313 scope 2）。**值不变，仍是 2**；变的是你在决定要不要
  调它时能依据什么。

  2026-09-02 按取证前预注册的判据实测：2 台设备 × 各 3 次连接，三相（early / late
  t≥5 s / control t≥30 s）× 两寄存器（`0x40`/`0x72`）× 30 次，`read_retries = 0`。
  **1080 次读零失败**——零超时、零回帧内容异常、零非超时错误。early 与 late 的失败率差
  6/6 全是 0 个百分点（门槛 15），耗时中位比 6/6 全在 1.00–1.01（门槛 1.5）。

  **结论是「测不出」，不是「不存在」。** 这两句话对调用方的意义完全不同：官方 SDK 那
  `Thread.sleep(5000)` 与 `0x72` 双发**既未被证实也未被否证**，N=30 分辨不了更小的差别。
  零失败另外给了一条边界——early 相 360 次读零失败，失败率 95% 置信上界约 0.83%——但
  **约束效应大小不等于否证效应存在**，别据此把重试去掉。

  经过、局限，以及「寄存器读往返耗时被 BLE 连接间隔量化成约 60 / 90 ms 两档，因而耗时
  判据的 1.5 倍门槛恰好等于多跨一个间隔」这条方法学事实，都记在 `docs/protocol.md` §10。

## 0.3.0 — 2026-08-28

### 升级须知

**两条破坏性变更对已知调用方都是安全的，但要知道它们存在。**

* **`Telemetry.read_version()` 在两个版本寄存器都为零时抛 `UnexpectedRegisterResponse`**，
  不再返回 `"0"`。
* **`ChipTime.__str__` 在字段不构成合法日期时不再打成时间戳样式。**

本仓库之外唯一已知的调用方是 gait-IMU，发布前核查过：`read_version`、`ChipTime`
**均零命中**，因此这两条对它没有影响。照 0.2.0 那次的做法把核查结果写在这里，
省得下一个读到「破坏性变更」四个字的人自己去查影响面。

#### ⚠ 三条不算破坏性变更、但会改变你看到的数字

这三条不需要改代码，但**会让同一段代码给出不同的数**。

1. **`0x71` 回帧的解码宽度从 4 个寄存器改成 8 个**（0.2.0 之后的修复）。协议固定一次
   回 8 个，此前按 4 个解，后 8 个字节被当成填充丢掉。**若你钉的 commit 早于
   `v0.2.0`，升级后 `read()` 返回的 `values` 长度会从 4 变成 8**，读 `0x3A` 一次就能
   拿到 HX/HY/HZ、读 `0x51` 一次拿到 Q0–Q3。真机实测与官方文档一致。

2. **`Battery.percent` 现在可能是 `None`。** 电量寄存器读到不可能的原始值 0 时，此前
   会被映射成看着正常的 `0%`。直接把 `percent` 序列化出去的调用方（例如
   `{"raw": battery.raw, "percent": battery.percent}`）会开始看到 `null`——那是
   「这个读数不可信」，不是「电量为零」，用 `battery.raw` 区分。

3. **连接就绪之前到达的实时数据帧不再交付。** `open()` 与重连路径上，从 `connect()`
   到该条连接的 `CONNECTED` 事件发出之间到达的帧被丢弃，计入**新增的**
   `DeviceStats.dropped_before_ready`。

   **它没有并进 `dropped_samples`。** 这一条对「`dropped_samples` 非零即判定本次采集
   不可信」这类调用方尤其要紧：那个判据的语义没有变（它仍然只表示消费者跟不上），
   但现在有了第二个计数，说的是另一件事。两者都不为零时先看新的那个。

### 破坏性变更

* **`Telemetry.read_version()` 在两个版本寄存器都为零时抛
  `UnexpectedRegisterResponse`**，不再走遗留分支返回 `"0"`。

  `"0"` 是一个看着像版本号的假值。选择抛异常而不是附标志：版本号只有「显示与比对」
  一个用途，不可信就毫无用处，与 `read_mac` 同理；经 `read_device_info` 的逐项容错后
  `DeviceInfo.version` 自然落成 `None`（RAY-305）。

* **`ChipTime.__str__` 在字段不构成合法日期时不再打成时间戳样式。** 全零回读此前会
  打出 `2000-00-00 00:00:00.000` —— 看着像个时间戳，而它不是。

### 新增

* **`DeviceStats.dropped_before_ready`** —— 在该条连接的 `CONNECTED` 事件发出之前
  到达、因此被丢弃的实时数据帧数。

  它与 `dropped_samples` 说的是两件事，**不要混着看**：那条是队列压力（消费者跟
  不上），这条与消费速度无关（流还没开始）。两者都不为零时先看这条——它意味着
  链路刚建立或刚恢复，那段数据本来就不该按当前配置去理解（RAY-311）。

* **连接期的链路信号强度。** `await device.read_rssi()`、
  `await device.telemetry.read_rssi()`，以及 `TelemetryPoller.rssi`
  （由 `PollerConfig.rssi` 控制，默认每 5 秒一次）。单位 dBm。

  本库此前只有**扫描期**的 RSSI（`DiscoveredDevice.rssi`，来自广播包），连上之后
  这个量就没了。而它与 `resync_count`、`dropped_samples` 不是同一类：那些是
  **结果**——链路已经出问题之后才动；RSSI 是**原因**侧的，是唯一一个能在丢包发生
  之前给出预警的量。两台设备同采时，只有它能区分「这台链路差」与「主机侧带宽
  不够」（RAY-310）。

  **只有 macOS 拿得到。** `bleak 0.22.3` 的 `BleakClient` 公开门面没有
  `get_rssi()`，`BaseBleakClient` 也没声明它，只有 CoreBluetooth 后端的实现类上
  有；BlueZ / WinRT / p4android 都没有。Linux 与 Windows 上恒为 `None`。**拿不到
  时是 `None`，永远不是 0**——0 dBm 是极强信号。

  `Transport.read_rssi()` 是基类上的**非抽象**方法，默认返回 `None`，所以自定义
  传输不实现它也不会坏掉。它约定**不抛异常**：超时、后端不支持、链路已断，全部
  落成 `None`——它的调用方是不该被单次读失败终止的轮询任务。

  新增 `BleTransport(rssi_timeout=...)` 与 `DEFAULT_RSSI_TIMEOUT`（2.0 s）。超时
  与内部串行化都不是可选的：bleak 的 `read_rssi()` 每个外设只有一个 future 槽，
  并发两次会让第一次**永久挂起**（与 RAY-177 同一类失败，只是发生在 bleak 里），
  而那个 `await` 自己没有任何超时。

  **尚未在真机上确认 macOS 是否真能读到已连接外设的 RSSI**：上述全部由读 bleak
  源码与运行时反射得到。判据已预注册在 `tools/probe_rssi.py`，需在已授权蓝牙的
  终端执行。


> ⚠ **下面这五条新能力，本库一条都没有在真机上验证过。** 字节构造照官方协议文档写死
> 并有离线测试，但「设备收到之后到底做了什么」没有实测数据。本库自己吃过这个亏：按
> 上游表把速率编码 `0x0A` 命名为 `HZ_125`，真机实测却是 99.29 Hz。**字节构造正确与
> 设备行为符合预期是两件事**，每个新入口的 docstring 都写了这句话（RAY-308）。

* **`SaveAction`** —— `FF AA 00 SAVE 00` 的三个官方取值：`SAVE_CURRENT`(`0x0000`)、
  `RESTORE_DEFAULTS`(`0x0001`)、`REBOOT`(`0x00FF`)。本库此前只用过第一个。

  `protocol.commands.save()` 现在接受一个 `SaveAction`，**默认仍是 `SAVE_CURRENT`**
  ——既有调用一个字都不用改。默认值留在最无害的那一档上是有意的：另外两个一个抹掉
  全部配置、一个断链重启，「不小心少传一个参数」不该变成一次出厂复位。
  另加两个便捷构造 `commands.restore_defaults()` 与 `commands.reboot()`。

* **`RegisterAccess.restore_defaults()`** —— 把设备打回出厂配置并保存。

  这不是「撤销我这次的改动」，是抹掉设备 flash 里的**全部**配置，包括别的软件写进去
  的。它正是 RAY-298 那台设备暴露的问题的解药：那台设备连上时 flash 里存着
  `BANDWIDTH=0x03`，与官方默认 `0x0004` 对不上，说明它被改过；在此之前想拿到一台
  状态确定的设备，只能逐个寄存器写一遍，而本库根本不知道「全部」是哪些。

  **它同时清空 `applied_writes`。** 不清的话，下一次自动重连会把刚被抹掉的配置重新
  放回去——调用方看到「复位了，过一会儿又变回来了」，而且没有任何报错。

* **`RegisterAccess.reboot()`** —— 重启设备。改完蓝牙名称之后必须重启才生效。

  **会断开 BLE 链路**，所以它发出即返回，不等待、也无从确认设备是否真的重启了——
  重启成功与链路断开在观测上是同一件事。`applied_writes` **有意不清空**：重启不改变
  flash 里的配置，重连后重放仍然是对的。与 `restore_defaults()` 的区别就是一句话：
  **设备还是不是你配置的样子。**

  这两个动作都**不走**普通写事务：`0x00` 本身就是保存指令，照「解锁 → 写 → 保存」
  三步走的话，`RESTORE_DEFAULTS` 之后那条保存会把刚复位掉的配置又存回去，`REBOOT`
  之后那条会打在一台正在重启的设备上。所以它们走「解锁 → 写」两步。

* **`CalibrationMode.ZERO_Z_AXIS`(`0x04`) 与 `CalibrationMode.ANGLE_REFERENCE`(`0x08`)**，
  以及对应入口 **`Calibration.zero_z_axis()`** 与 **`Calibration.set_angle_reference()`**。

  `zero_z_axis()` **会先读回 `0x24` 确认设备在 6 轴算法下**，不是就抛
  `ConfigurationError`。官方注明这条指令需先切 6 轴才生效——而 9 轴下它**不报错也
  不生效**，调用方以为归零了、数据却没变。多花一次 BLE 往返换掉这种静默失效是划算
  的；要跳过检查就用通用 `registers.write()`。本库此前只在 `Register.ALGORITHM` 的
  文档里提过这条门控关系，却没有对应入口——知道有这回事，却用不了。

  `set_angle_reference()` 标定的是**姿态参考**（把当前姿态当零点，与设备水不水平
  无关），与 `calibrate_acceleration()` 标定**传感器零位**（要求水平静置）不是一回事。
  官方注明发送后需再发保存指令，本库的写事务本来就以保存收尾。

  两者都是**动作不是配置**，`remember=False`，不参与重连重放——重放一次归零就是在
  重连那一刻的朝向上重定零点。

* **`WT901Device.set_bluetooth_name(name)`** —— 改设备的蓝牙广播名。

  **这不是寄存器写入**：封装是 `WT` + 名称 + `\r\n`（变长），不走「解锁 → 写 →
  保存」，也没有对应的寄存器地址可读回。所以它在 `WT901Device` 上而不是
  `RegisterAccess` 上。

  `name` 是完整名称，必须以 `WT` 开头（官方：那两个字符「不可修改，否则会导致 APP
  搜索不到」），之后最多 14 字节 ASCII。长度上限能被帧长佐证：2（协议头）+ 2（`WT`）
  + 14 + 2（`\r\n`）= **20 字节，正好用满 BLE 单次上传上限**。非 ASCII 直接拒绝——
  多字节字符会让「几个字」与「几个字节」对不上，超出部分被链路截断，结果是「名字改
  了但不是我要的名字」，比直接拒绝难查得多。

  **改名后必须重启才生效**（`registers.reboot()`），且要按新名字重新搜索。

  它与寄存器事务**抢同一把锁**（新增 `RegisterAccess.exclusive()`）：这条指令不是
  寄存器写入，但打在同一条 GATT 写特征上，两条写同时在飞会让 bleak 的 CoreBluetooth
  后端永久挂起（RAY-177 真机确认）。「它不是寄存器写入」不是绕过那把锁的理由。

  ⚠ **docstring 明写：改名不是设备自报的身份，不能替代 `read_mac()`。** 同批设备
  广播名重复是 RAY-279 立项的理由之一，改名看起来能解决它但解决不了——名字是主机写
  进去的、不参与任何校验，两台设备被改成同一个名字本库无从发现；而且改名不可逆地丢
  掉了旧的找回路径。改名的正当用途是**给人看**，现场分辨哪台是左脚哪台是右脚。

* **`Mounting.VERTICAL` 补上官方硬约束**：`垂直安装（必须坐标轴的 Y 轴箭头朝上）`。
  本库此前只抄了「垂直安装」四个字，把括号里那句丢了。装成垂直但 Y 轴没朝上，**设备
  不会报错**——链路、速率、丢包全部正常，只有姿态解算基准是错的，数据一直偏。
  **本库无法替调用方检查这一条**（设备不知道箭头朝哪），文档是唯一的防线（RAY-308）。

* **`ConnectionState.CONFIG_REPLAY_FAILED`** —— 「链路连上了，但配置没恢复，这条
  连接已被断开」。与 `RECONNECT_FAILED` 的差别是调用方的处置：那条说「还没连上，
  等着」，这条说「别用这批数据」。它不是终态，重试耗尽时照常发 `RECONNECT_FAILED`。
  见上面「修复」一节（RAY-306）。

* **`Bandwidth` 补齐上游表的全部七档**：新增 `HZ_188`(`0x01`)、`HZ_98`(`0x02`)、
  `HZ_10`(`0x05`)、`HZ_5`(`0x06`)，与原有的 `HZ_256`/`HZ_42`/`HZ_20` 一起覆盖
  `0x1F` 的 `0x00`–`0x06`。`0x07` 及以上仍然拒绝。

  在此之前只登记三档，需要 98 Hz 的调用方只能用通用 `write(0x1F, 0x02)` 绕过——
  那会同时绕开「未登记编码一律拒绝」这道白名单，比登记它更糟。

  **七个标称值全部来自维特通用编码表，本库一档都没实测过**，新增的四档连「设备
  接受它」都没验证过。成员名在这里是**转述**（「上游表把这个编码标为 98 Hz」）而
  非断言（`ReturnRate.HZ_100` 意思是「本库量到过 100 Hz」）——同一张通用表在速率上
  被证伪过一次（`0x0A` 标 125 Hz、实测 99.29 Hz），没有理由假定它在带宽上都对。
  要拿这些数字做采样率/抗混叠的设计决策，先自己测。详见 `docs/protocol.md` §6.2
  与 `Bandwidth` 文档（RAY-304）。

* **`open_recording()` / `RecordingReader` / `RecordingHeader`** —— 录制文件的流式
  读取入口，峰值内存与文件大小无关。

  ```python
  with open_recording(path) as reader:
      print(reader.header.device_id)   # 头部在打开时就有，不必读完整份文件
      for chunk in reader:             # 逐行产出
          ...
  ```

  `read_recording` 的行为与签名不变，**并且现在建立在同一段解析代码之上**——两条
  路径对末行截断与中间行损坏的判定因此不可能分叉，这是结构上的保证，不是靠测试
  比对出来的。

  `RecordingReader.truncated` 是 `bool | None`：**迭代完成之前是 `None`**。流式读取
  在读到文件尾之前无从知道末行完不完整，给 `False` 就是把「还不知道」说成「没截断」
  ——与 `SerialNumber.value`、`ChipTime.is_plausible` 同一条线。整份读完之后构造出来
  的 `Recording.truncated` 仍然是 `bool`。

  **不是尾随读取**：它读的是一份已经写完的文件。跟着写入方一起读涉及「读到一半的行
  下次要重读」这类语义，本入口不提供。

  `tools/record_session.py` 已改用它——那里只需要段数、字节数与时长三个统计量，单趟
  扫描就够（RAY-295）。


* **`Bandwidth.HZ_42`（`0x1F` = `0x03`）。**

  《BS-BT91 硬件适配与时间同步方案 v0.1》§3.1 七步序列的第 ③ 步要的就是这一档。在此
  之前它会被 `set_bandwidth()` 拒绝，下游只能用通用 `write(0x1F, 0x03)` 绕过——而绕过
  正是 RAY-241 / RAY-291 反复论证要避免的反模式，且会绕开「未登记编码一律拒绝」这道
  防线。七步序列现在可以被具名 API 完整表达。

  未列出的编码（`0x01`=188、`0x02`=98、`0x05`=10、`0x06`=5 Hz）仍然拒绝：拒绝防的是
  **误**写，与证据强度无关。

* **`ChipTime.is_plausible` 与 `Quaternion.is_plausible`** —— 寄存器整块回读全零是
  本器件有据可查的现象（`docs/protocol.md` §10），这两处此前会交出看着正常的假值：
  `2000-00-00`（日历上没有第 0 月第 0 日）与 `(0,0,0,0)`（模为 0，不表示任何朝向）。

  **这两处附标志而不抛异常，理由不同于 `read_version`：**

  - `ChipTime`：本方法要回答的正是「设备是不是刚上电」。一台时钟从未设过的设备
    **可能本来就报全零**，而本库没有核实过它上电时报什么。抛异常会把「时钟未设」
    和「这次没读出内容」一起抹掉，而前者正是调用方问的那件事。
  - `Quaternion`：原始值是判定成因所需。**归一化前先看这个属性** —— 对模为 0 的
    四元数归一化得到 NaN，它会一路飘进姿态解算。

  判据都只看「原始值是否全零」/「月日是否可能存在」，不划容差线、不检查模是否接近
  1：器件给的是定点数，正常读数的模也只是接近 1，划线就是发明没实测过的规则。

  `read_temperature()` **有意不在此列**：0 °C 是可能的真实测量。本库只挡不可能的值。

  `TelemetryPoller` 的行为随之明确：不可信的读数照常写进属性，可信与否随值一起走，
  不做「保持上一次的有效值」—— 那会让陈旧值冒充当前状态，比一个自带标志的新值更难查。

### 修复

* **连接就绪之前到达的实时数据帧此前照常变成样本交付。**

  `open()` 与重连路径上都有这么一段窗口：`connect()` 一返回通知就已订阅、字节
  随时会到，而这条连接的 `CONNECTED` 事件还没发出。重连路径上这段窗口尤其长
  —— 中间隔着一整次配置重放，每条寄存器约 0.2 s。离线复现：配两条寄存器、
  200 Hz，**0.397 s 内有 69 个样本在 `CONNECTED` 之前就被交付**，seq 0–68。

  两件事同时错了。其一，`ConnectionEvent` 的 docstring 写着上层必须据它判断样本
  序列的连续性，而这些样本恰恰跑在那条事件前面：按事件划分连接段的调用方会把
  它们划进上一条连接；按 seq 单调性检测丢包的会看到 `…4000, 0, 1, 2…`，而当时
  收到的最后一条事件还是 `reconnecting`。其二，这段数据是设备在**配置尚未恢复**
  时产出的——它停在自己的上电状态（出厂 10 Hz），采样率与其余部分不同，而没有
  任何一处标记它。

  现在这些帧被**丢弃**，不入队也不缓冲（缓冲后补发只是把两种采样率换个时间混进
  同一条序列），并计入新增的 `DeviceStats.dropped_before_ready`。`_seq` 不因它们
  推进，所以 `CONNECTED` 之后第一个交付的样本仍是 seq 0，seq 的缺口从此只表示
  背压丢弃。寄存器通道**不受门控影响**：`0x71` 回帧照常解码分发，否则配置重放
  自己就会被挡死。

  保证的边界写在 `ConnectionEvent` 的 docstring 里：样本不会在 `CONNECTED`
  **发出之前被造出来**；但 `samples()` 与 `events()` 是两条独立队列，跨队列的
  消费先后仍不归本库管（RAY-311）。

* **重连后的配置重放一旦失败，整条重连语义静默失效。**

  重连钩子（默认接的是 `RegisterAccess.replay()`）抛出的异常直接冲出重连循环：
  `CONNECTED` 事件永远不发，而 `_seq` 已经归零、`stats.reconnects` 已经加一、
  传输层其实已经连上、样本也在流——四个观察互相矛盾，调用方只看得到最后一条
  `reconnecting`。设备停在它上电时的状态（出厂 10 Hz），应用以为是自己配的那个
  速率，**没有任何一处报错**。唯一的痕迹是 asyncio 在任务被 GC 时打的
  「Task exception was never retrieved」，调用方没配 logging 就完全看不到。链路
  刚恢复正是最容易再抖一下的时刻，而重放的第一条就是解锁指令。

  现在钩子的异常不再逃逸：本库自己的 logger 记一条、发
  `ConnectionState.CONFIG_REPLAY_FAILED`、**主动断开链路**，这次重连整体算失败，
  交回既有的指数退避继续重试（实测下一次尝试能把配置补上）。连着但配置错比断开
  更危险——它会持续产出看着正常的数据。`stats.reconnects` 相应改为只在「连上
  **且**配置重放成功」之后 +1，统计与事件流不再互相矛盾（RAY-306）。

* **`note` 里含 U+2028 / U+2029 时，录制文件读不回来。**

  头部用 `ensure_ascii=False` 写出，这两个字符原样落进文件；而读取用
  `str.splitlines()` 切行，它在这两个字符上断行，于是头部被劈成两半、后半段又被
  当成数据行。JSON Lines 的行分隔符只有换行，`splitlines()` 认的那一串（`\r`、
  `\x0b`、`\x0c`、`\x1c`–`\x1e`、`\x85`、U+2028、U+2029）比它宽。

  改成按文件行读之后不会了。这是 `open_recording` 重构顺带修掉的，不是它的目标，
  但同样有测试钉住（RAY-295）。


* **一次 `0x71` 应答携带 8 个寄存器，本库此前按 4 个解，多出的一半被静默丢弃。**

  `RegisterResponse.values` 因此从 4 个元素变成 8 个。取前几个的调用方不受影响；
  `value_at()` 的覆盖范围翻倍（读 `0x3A` 现在也能取到 `0x40` 温度），越界行为不变。
  若有调用方在断言 `len(values) == 4`，那个断言现在会失败——它断言的本来就是一个
  错误的事实。

  连带 `Telemetry.read_serial_number()` **少跑一趟 BLE 往返**：序列号占 6 个寄存器，
  过去因为「一次只回 4 个」要读 `0x7F` 与 `0x82` 两次，现在一次读全。寄存器读与
  `0x61` 实时流抢同一条链路，省下的是采样带宽。返回值不变。

  帧长本来就算得出来：负载 18 字节 = 2 字节起始地址 + 16 字节寄存器区 = 8 个寄存器，
  按 4 解会有 8 个字节没有说法，而手册摘要 §3.3 也一直写着 8 个。**这不是上游资料
  缺陷，是本库自己读错了**，而且离线测试永远发现不了它：构造帧的测试工具与被解码的
  代码用的是同一个错误常量，两边一起错就一起「通过」。

  真机核实用的是**语义可自证**的量（`tools/probe_register_width.py`，2026-08-27 一台
  WT901BLE67，证据 `ray-292/register-response-width/acceptance/`）：读 `0x2E`，第 5–6
  个位置解出的分/秒与毫秒夹在前后两次直读 `0x30` 的结果之间；读 `0x3A`，第 7 个位置
  是 30.04 °C 而直读 `0x40` 为 30.03 °C。两者都落在原先被丢弃的那 8 个字节里，各 3 轮
  全部成立。真机原始负载已作为 fixture 进 `tests/test_register_response_width.py`
  （RAY-292）。

### 更正

* **`docs/protocol.md` 补上官方写了、本库一条都没记的设备知识；§5 寄存器表改为如实
  反映官方表的范围。**

  本库没有任何 API 变化，但下游读文档得到的结论会变，所以记在这里。

  * **坐标系与欧拉角顺序**（新增 §1.1）：东北天坐标系，X 向左、Y 向前、Z 向上；
    欧拉角旋转顺序 **Z-Y-X**。`Roll` 对应 X 轴这件事本库到处在用，却从来没写下依据。
  * **俯仰角 ±90° 会折回，而且三轴耦合**（新增 §1.2）：俯仰角超过 90° 会变换到小于
    90°，**同时让 X 轴角度大于 180°**；Y 轴接近 90° 时即使只绕 Y 轴转，X 轴角度也会
    大幅变化。**这是欧拉角表示姿态的固有特性，不是设备故障**——不写，下游看到「只绕
    一个轴转、另一个轴的读数却在跳」会去查链路、查解码、查标定，那三处都没有问题。
  * **§5 寄存器表重写**：`5.1` 照录官方地址表（含本库未开放的 `0x04` BAUD、
    `0x05`–`0x0A` 加速度/角速度零偏、`0x0B`–`0x0D` 磁场零偏、`0x0E`–`0x11` D0–D3MODE、
    `0x34`–`0x39` AX–GZ、`0x3D`–`0x3F` Roll/Pitch/Yaw，以及 `0x02`/`0x12`–`0x1B`/
    `0x49`–`0x50` 三段保留区），每一行标注**开放 / 登记 / 保留**；`5.2` 列官方地址表
    里没有但设备确实有的那些；`5.3` 记官方有指令而本库还没有入口的取值。
  * **`5.4` 记下「官方寄存器地址表本身并不完整」**：`0x1F`/`0x23`/`0x24`/`0x27`/
    `0x64`/`0x69` 都不在那张表里，却在同一份文档的指令小节里写得清清楚楚，其中
    `0x1F` 还给了完整寄存器规格。**是那张表漏了，不是本库编的**——不写下来，下一个人
    会以为本库多写了东西然后去删。
  * **`5.5` 记下 `Mounting.VERTICAL` 的官方硬约束**：垂直安装**必须坐标轴的 Y 轴箭头
    朝上**。装错设备不报错，只有姿态解算基准是错的（代码侧 docstring 在 scope 3 补）。
  * **顺手修掉 §5 里 `0x1F` 那一行的三档残留**：它还写着「开放 `0x00`/`0x03`/`0x04`」，
    而同一文件的 §6.2 早已是七档（RAY-304 只改了后者）。同一份文档里两处互相矛盾，
    读者信哪个全看他先翻到哪一页（RAY-308）。

* **电量百分比阶梯：协议文档与官方 SDK 给了两张不同的表，本库此前只记了 SDK 那张。**

  协议文档的「读取电量」小节给的是一张 **21 档、5% 一级**的表（并给出电压对照，证实
  「原始值 ≈ 电压 ×100」）；本库实现的是 SDK 那张 12 档的表。**同一个原始值最多差
  25 个百分点**——例如原始值 `370`，本库给 20%，协议文档给 45%。

  **本库的行为一个字没改**，两张表都来自官方，这是又一处上游内部矛盾，按方针 2 待
  单独立项以实测定论。文档现在明说这一点，并提醒：**`Battery.percent` 只适合做粗略
  的「电快没了」提示，不要拿它做续航估算或换电阈值**；`Battery.raw` 不受影响，那是
  设备直接给的数（RAY-308）。

* **磁场量纲那条的取证判据修正了。** scope 1 写的「放进已知场强（地磁 25–65 µT）看
  哪份换算算得出合理数量级」**只分得开一份**：协议文档自己的例子解出 `Hx=360` mG，
  按 mG 读是 36 µT、按 SDK 的 ×0.15 读是 54 µT，**两个都落在地磁范围内**，只有 Python
  示例的 `/120`（3 µT）会被排除。要分开前两份需要已知且可控的场强，或拿已标定的磁力
  计同点比对。这一点写进了 §5.7，免得将来做一次分不出结果的实验（RAY-308）。

* **`Bandwidth` 七档的出处写低了一档：那七个数印在本型号自己的协议文档里，不是只见于
  维特跨型号的通用编码表。**

  协议文档的 `0x1F` 条目写着 `默认值 0x0004`，`BANDWIDTH[3:0]` 为
  `0x00:256Hz 0x01:188Hz 0x02:98Hz 0x03:42Hz 0x04:20Hz 0x05:10Hz 0x06:5Hz`——七档与
  本库登记的完全一致。**证据强度上调，「本库一档都没实测过」不变**：出处更硬不等于
  数字被量过，这两件事分开算。

  **顺带拆掉一个不成立的类比。** 此前 `Bandwidth` 的残留风险写作「同一张通用表在速率
  上已经被证伪过一次（`0x0A` 标 125 Hz、实测 99.29 Hz），没有理由假定它在带宽上就都
  对」。被证伪的是通用表，而带宽七档出自协议文档，两者不是同一张表。风险本身没有消失，
  换成了三条能站住的理由：本库一档都没量过；协议文档自己也把加速度/角速度并成一列；
  **唯一一次尝试测量的结果与文档对不上**（标称 20 Hz 的 `0x04` 测出 10.9 Hz，那次自
  校验没过，所以既不能推翻文档也不能算作支持它）。

  **`set_bandwidth` 拒绝信息的文本变了**：「不在上游表列出的 0x00-0x06 内 …（标称值
  来自维特通用编码表…）」→「不在协议文档列出的 0x00-0x06 内 …（标称值载于本型号官方
  协议文档…）」。异常类型仍是 `UnsupportedRegisterError`，按类型捕获的调用方不受影响；
  按文本匹配的会受影响。**「本库一档都未实测」这句照旧在信息里**（RAY-308）。

* **`ReturnRate` 排除 `0x0A` 的理由变硬了：本型号官方速率表根本没有这个编码。**

  协议文档的速率表只有 `0x09:100Hz`、`0x0B:200Hz`。「`0x0A` = 125 Hz」出自维特跨型号
  的通用编码表，本型号文档从未这么说。此前本库给的理由只有「实测 99.29 Hz 对不上标称
  125 Hz」——那条仍然成立，但它现在是第二条证据而不是唯一一条。

  **枚举一个字没改**，`0x0A` 照旧不在里面（RAY-308）。

* **`Register.BANDWIDTH` 补记官方默认值 `0x0004`（标称 20 Hz），并记下它与实测的对照。**

  RAY-298 取证时那台设备连上读回的是 `0x03`，与出厂默认对不上——**说明那台设备被改过，
  不是出厂态**。这条不是矛盾而是一条有用的事实：「读回什么就是出厂什么」这个假设在本
  器件上已经被证否一次，任何依赖出厂默认的推断都要先确认设备没被动过。它同时把
  `Register.MOUNTING` 那条「不写就等于依赖设备残留配置」从假想风险变成了实际遇到过的
  情况（RAY-308）。

* **磁场量纲：三份上游资料互相矛盾，本库此前只记了两份。**

  本型号官方协议文档说原始值单位**就是 mG**（`Hx=((HxH<<8)|HxL)`，并注明「和电脑上位机
  显示的单位不一样」），即 µT = raw × 0.1，**与 `0x72` 无关**；官方 C#/Android SDK 按
  `0x72` 分档（type 2 → ×0.15，本库采用）；官方 Python 示例写死 `raw / 120`。三者两两
  都对不上，而第一份的出处比 SDK 更硬。

  此前 `magnetic_field_to_ut` 的 docstring 把第三份写成「该示例自身的陈旧实现」一句带
  过，只字未提第一份——那不足以描述实情。

  **换算行为一个字没改**，仍按 SDK 分档，理由是它能解释 `0x72` 这个寄存器为什么存在
  （若原始值恒为 mG，那个寄存器就没有用处，而 C#、Android 两份独立实现都在读它）。
  **但 docstring 现在明说这是权衡下的暂定选择而不是结论**，并按本项目方针「官方资料
  互相矛盾处先立 Issue、以实测定论」注明要单独立项取证，不在实现里选边。拿返回值做
  绝对场强判断的调用方要知道它可能整体差 1.5 倍或 18 倍；只做相对变化或方向的不受
  影响（RAY-308）。

* **`docs/protocol.md` 与 `registers.py` 此前声称全部条目「由官方 SDK 源码逐条核实」，
  那句话对六组寄存器不成立。**

  2026-08-28 把官方 SDK（`WITMOTION/WitBluetooth_BWT901BLE5_0`，钉住 commit
  `9efaab0fdd6a06dc807bf80402e58aa91b431c6f`）全量扫了一遍：它触及的寄存器只有读
  `0x03` `0x2E` `0x3A` `0x40` `0x51` `0x64` `0x72` 与写 `0x00` `0x01` `0x03` `0x1F`
  `0x27` `0x69`。`0x23` 安装方向、`0x24` 算法、`0x30`–`0x33` 芯片时间、`0x66` MAC、
  `0x7F` 序列号、`0x96` 输出内容这六组，SDK 一次都没碰过。前五组有本型号协议文档
  背书（只是出处标错了），`0x72` 反过来只有 SDK 背书。

  那句统一的核实声明把「有文档背书」与「有代码背书」混成了一句话，还顺带把一个**两份
  上游资料都查不到**的地址盖进了「已核实」。现在**没有整篇/整模块统一的来源声明了**，
  出处逐项写在自己那一条上，`docs/protocol.md` 新增 §0 给出对照表。

* **`Register.SERIAL_NUMBER`（`0x7F`）在协议文档与官方 SDK 里都查不到，此前未注明。**

  它进本库时没有留下出处。与此同时它在真机上四台次全为零（`ray-172` 两台不同主机、
  `ray-279` 两台不同设备），此前记的解释是「这批器件没烧写序列号」。**更简单的解释是
  本型号根本没有这个寄存器，读一个未定义地址就返回零。** 两种解释目前分不开。

  `docs/protocol.md` §10 那条现象**不删**——它是实测出来的，删掉会丢真实数据；改的只
  是「它意味着什么」这一层，并注明成因待 `ray-309` scope 2 用实测定论（读官方标为
  保留/未定义的地址与 `0x7F` 比对）。

  `Telemetry.read_serial_number()` 的**行为一个字都没改**，只在 docstring 里写明这层
  不确定：它如实报告读到了什么，而不是替调用方断言那是什么（RAY-309）。

* **`docs/protocol.md` 新增 §11 记录这次 SDK 全量比对**，含 SDK 触及的寄存器全集、
  「本库是其超集」这个结论、副本位置与钉住的 commit，以及**这次比对没覆盖 Unity_C#
  与 Windows_C# 的 WitSdk 主体**——写下边界，免得下一个人以为比对是完整的（RAY-309）。

* **`MergeStats.out_of_order` 的文档说的不是代码在算的东西。** 文档写「``t_host``
  早于**上一个已发样本**的样本数」（相邻比较），而实现比的是一条**高水位线**——
  乱序样本不推进 `_last_t`，所以它数的是「早于此前已发出的最大 `t_host`」的样本数。

  两个定义能差好几倍，而真实的 BLE 抖动形状恰恰是它们分岔的地方：一条流卡住几个
  采样周期再把攒下的一次吐出，相邻逆序只有 1 处，低于高水位的却是攒下的每一个。
  这不是措辞问题——模块文档把 `MergeStats` 定为「判断输出可不可信的入口」，README
  也让调用方据此判断这次采集能不能用，「`out_of_order / emitted` 低于 1% 就算可用」
  这类阈值在两种定义下含义完全不同。

  **保留实现、改正文档**：高水位那个定义数的是「调用方需要重排的样本总数」，对要做
  时间对齐的下游更有用；相邻逆序数会把一串连续迟到的样本只算 1 处，低估下游的工作
  量。被否决的替代是改成相邻比较——那要让 `_last_t` 在乱序时也推进，换来的数只能
  回答「抖了几次」，而那个问题 `latency_flushes` 已经回答了，等于用一个字段的语义
  去换一份重复信息。**行为一字未改，`out_of_order` 的取值与此前完全一致**；受影响
  的是按旧文档去理解这个数的调用方。

  `_last_t` 不被乱序样本推进这一点在代码里也写明了，否则下一个读到的人会当成 bug
  去「修」。同时新增一条判别性测试：现有三条断言 `out_of_order` 的用例都只有一个
  迟到样本，两种定义在那里都等于 1，区分不了；新用例用一串连续迟到的样本把语义钉
  死（离线复现，不需要真机）。顺带补上 `max_latency=0` 的含义（「一个样本都不等」，
  实际上等于不归并；想要等齐是 `None`），此前构造函数只说了不能为负（RAY-307）。

* **`Bandwidth` 此前声称「只登记已核实的两档」，那句话不成立——三档的标称值一个都没
  被实测过。**

  `ray-171` 的验收清单本就记着「`BANDWIDTH` 未逐档探测」；早期证据里那句「带宽已设为
  20 Hz，读回 `0x04`」只是把编码写进去再读回来，与截止频率是多少无关。`HZ_20` 与
  `HZ_256` 随 RAY-168 从官方 SDK 抄入，数字来自维特通用编码表。

  这不是措辞洁癖：本库把「已核实」当成一个有分量的词用——`ReturnRate` 排除 `0x0A` 靠
  的就是它（通用表标 125 Hz，实测 99.29 Hz）。这个词用错地方，会让读者以为带宽档位
  也经过同等强度的验证。docstring、`docs/protocol.md` 与拒绝信息的措辞一并改正，并
  新增一条测试挡住它退回去。

  **拒绝信息的文本变了**：不再说「未在真机上核实 / 当前已核实」，改为「未登记 / 当前
  登记」。异常类型仍是 `UnsupportedRegisterError`，按类型捕获的调用方不受影响；按文本
  匹配的会受影响。

  真机上确立的事实（2026-08-27，一台 WT901BLE67，`tools/probe_bandwidth.py`，证据
  `ray-298/bandwidth-42hz/acceptance/`）只有两件：设备接受三个编码并照常出数；三档
  截止频率单调有序（20 Hz 处 `0x03` 衰减 −1.4 dB 而 `0x04` 为 −7.0 dB，50 Hz 处
  −2.3 dB vs −13.1 dB）。用噪声底功率谱比值量截止频率的那次尝试**自校验没过**——标称
  20 Hz 的 `0x04` 测出 10.9 Hz，因此同一次测出的 `0x03` = 35.6 Hz 同样不算数。经过与
  局限记在 `docs/protocol.md` §6.2（RAY-298）。

## 0.2.0 — 2026-08-27

### 升级须知

**`Telemetry.read_serial_number()` 的返回类型变了**，从 `str` 变成 `SerialNumber`。
调用方要改用 `.value`（`str | None`）与 `.is_plausible`，理由见下方「破坏性变更」。

本仓库之外唯一已知的调用方是 gait-IMU，升级前核查过：它**零处使用**该 API，因此
这次升级对它是安全的。把这条写在这里，是为了让下一个读到「破坏性变更」四个字的人
不必自己去查影响面——**运气好不等于没风险**：假如它当初用了序列号，这个版本就升不动。

其余改动都是新增或行为收紧，没有需要调用方改代码的地方。

### 修复

* **一条流长期静默时，`merge()` 不再把存活流的产出压到 `1 / max_latency`。**

  等待预算原本按流反复支付：每发一个样本都要重新等一遍那条不说话的流，于是存活
  流被钉死在一个与设备速率无关的绝对上限（默认 20 Hz）。真机上双设备 200 Hz 关掉
  一台，存活那台从 198.6 Hz 塌到 19.5 Hz，其余九成样本在设备队列里被静默丢弃
  （RAY-190）。它满足「另一台没有停」的字面要求，交给上层的却是一条十抽一的流。

  现在超出预算的流被移出等待集，直到它自己再开口；这段时间合流按存活流全速推进。
  该缺陷自 0.1.0 起存在，与采样率无关（100 Hz 下同样塌到 20 Hz）。

### 破坏性变更

* **`Telemetry.read_serial_number()` 改为返回 `SerialNumber` 而不是 `str`。**

  全零的回读此前会变成空字符串 `""`，在 `DeviceInfo` 里与「这一项没读到」长得一模
  一样。而这不是假想情形：本器件真机上读到过逐字节全零的序列号（RAY-172 记过一次，
  RAY-279 的取证里两台设备再次全零）。下游拿 `""` 当绑定键会让**每台设备的键都相同**
  ——两台设备安静地互相冒充，正是 RAY-279 反复警告的那种失败（RAY-293）。

  ```python
  serial = await device.telemetry.read_serial_number()
  serial.value        # str | None，全零时为 None
  serial.raw          # bytes，始终给出，判定成因需要它
  serial.is_plausible # 这次读数是否可能是真实内容
  str(serial)         # 可打印：全零时是空串
  ```

  形状与 `Battery` 一致（RAY-182 的 `raw = 0`），而**不是**与 `read_mac` 一致（那里
  全零直接抛异常）。区别是有意的：MAC 只有身份一个用途，不可信的 MAC 毫无用处；
  序列号是设备信息，「它读出来是全零」本身就是诊断线索，值得原样交出来。

  `DeviceInfo.serial_number` 的类型不变（`str | None`），新增 `serial_number_raw`
  区分两种 `None`。非 ASCII 字节以 `\uFFFD` 替换的行为不变——掺乱码与全零是两回事。

### 新增

* **`SerialNumber`** —— 见上，从包根导出。

* **`Mounting`、`Settings.mounting`、`RegisterAccess.set_mounting` / `read_mounting`**
  —— 安装方向（寄存器 `0x23`）现在能具名表达，与 `set_output_rate` / `set_bandwidth` /
  `set_algorithm` 同构。

  此前只能走通用的 `write(0x23, 0)`。编码不像 `0x24` 那样反直觉（0 水平、1 垂直），
  但**写错同样不报错**：装反了只是让姿态解算的重力轴对不上，数据一直偏，而链路、
  速率、丢包这些可观测量全部正常（RAY-291）。

  **即使写的是出厂默认值也不要省掉它。** 下游适配文档给的是七步下发序列，其中这一步
  多半是幂等的——它的价值不在于改变什么，而在于让「一份配置快照完全决定设备状态」
  成立。省掉它就等于依赖设备残留的配置，而模块会被别处用过、配置又固化在 flash。
  这层理由写在 `Register.MOUNTING` 与 `set_mounting` 的文档里，并有测试钉住。

  按**配置型**处理（`remember=True`），进 `applied_writes` 并被 `replay()` 重新下发，
  与 `0x24` 同类。`settings()` 的下发顺序为 速率 → 带宽 → 算法 → 安装方向，与适配
  文档 §3.1 一致并有测试钉住。

* **`read_recording(path, *, tolerate_truncated_tail=False)` 与 `Recording.truncated`**
  —— 崩溃截断的录制文件不再整份不可读。

  进程被 `kill -9`、掉电、或写到一半被打断时，文件的最后一行必然是残行，而严格
  解析会让**此前全部完好的数据一起失效**。真机上一份 30 分钟 200 Hz 的录制截断
  后，前 633 行完好，一行都取不出来（RAY-280）。而采集轮次中途被打断是常态。

  **默认行为不变，仍然严格**：静默容忍会把「这份文件坏了」变成一个没人注意到的
  事实。要容忍就得明写 `tolerate_truncated_tail=True`，拿到结果还要看
  `Recording.truncated` 才知道发生了什么。

  只容忍**最后一行**的 JSON 解析失败。中间行损坏说明文件被改过或拼接过，照旧拒绝
  并指明行号；末行若能解析出 JSON 但时刻倒退、hex 非法，那是损坏而不是截断，两种
  模式都拒绝。这个边界是准确的而不是保守的：数据行 `{"hex":"…","t":…}` 的任何真
  前缀都不是合法 JSON，所以「末行解析不了」恰好等价于「末行被截断」，有测试钉住
  这条性质。头部自身被截断不在容忍范围内——那种文件没有可救的数据。

  `truncated` 描述的是**来源文件**而不是这份数据，因此不进文件格式：把救回来的
  录制写出去得到的是一份完好的文件。`ReplayTransport` 不需要另开开关，救回来的
  `Recording` 直接传给它的构造函数即可。

* **`Telemetry.read_mac()`、`DeviceInfo.mac`、`Register.MAC`** —— 设备自报的蓝牙
  地址（寄存器 `0x66`）现在能具名读到，返回 `XX:XX:XX:XX:XX:XX`。

  此前本库**没有任何一个可跨主机持久化的设备身份**：`DiscoveredDevice.address` 在
  macOS 上是 CoreBluetooth UUID（它自己的文档就写着「不要跨主机持久化」），广播名
  同批次重复（两台设备都叫 `WT901BLE67`），序列号真机上读到过逐字节全零。下游要把
  模块绑到「左脚/右脚」并跨主机保持这个绑定，只能自己发 `FF AA 27 66 00` 并解析
  `0x71` 应答——那是把设备知识推回业务仓库（RAY-279）。

  **字节序是反的**：`0x66`–`0x68` 按小端取出的 6 字节是地址的空口顺序，显示时整体
  倒过来。手册没给这个排布；它由 2026-08-27 两台真机的实测应答推定——四种可能排布
  里只有这一种让两台设备都得到合法的 BLE 随机静态地址。**在 Windows/Linux/Android
  主机上看一眼同一台设备的 MAC 即可证实或推翻**，依据与推理写在 `read_mac` 的文档
  与 `docs/protocol.md` 里。

  回读全零抛 `UnexpectedRegisterResponse` 而不是返回 `00:00:00:00:00:00`：序列号
  寄存器就出现过全零（RAY-172），MAC 若也如此，一个「所有设备都相同」的绑定键会让
  两台设备安静地互相冒充。`read_device_info()` 里这一项照旧逐项容错，读不到为 `None`。

* **`AlgorithmMode`、`Settings.algorithm`、`RegisterAccess.set_algorithm` /
  `read_algorithm`** —— 6 轴/9 轴姿态解算算法（寄存器 `0x24`）现在能具名表达，与
  `set_output_rate` / `set_bandwidth` 同构。

  此前只能走通用的 `write(0x24, 1)`，这把「1 表示 6 轴相对航向」这条设备知识推回给
  每个调用方。**而这个编码与直觉相反：值大的 1 对应的是轴少的那个。** 写反了设备
  不报错，只会让航向从相对航向变成依赖磁力计的绝对航向，在室内金属环境下悄悄劣化
  ——数据里看不出来（RAY-241）。

  按**配置型**处理：进 `applied_writes`，重连后由 `replay()` 重新下发。这与校准写入
  相反（RAY-173：校准是一次性动作，重放会重做零位标定）；算法模式必须重放，否则
  重连后航向语义可能在调用方毫不知情的情况下改变。

  手册注明「Z 轴角度归零」需先切到 `SIX_AXIS` 才生效，该门控关系写在
  `Register.ALGORITHM` 与 `set_algorithm` 的文档里。

* **`MergeStats.emitted_while_stalled`** —— 至少有一条流处于停滞状态时发出的样本
  数。这段输出是单边的：顺序只在存活流之间成立。合流不会因此变慢或报错，所以不看
  这个字段就发现不了。

  `latency_flushes` 的含义随之更干净：一条持续静默的流只贡献一次，不会被刷爆。

### 变更

* **`battery_percent()` 与 `Battery.percent` 现在可能是 `None`。** 原始值是电压
  ×100，非正数不可能是一次真实测量；此前阶梯表把它映射成 0%，也就是把一个无效
  读数说成「没电了」。真机上确实读到过原始值 0（RAY-182）。

  新增 `Battery.is_plausible`。`DeviceInfo` 里两种 `None` 靠 `battery_raw` 区分：
  它为 `None` 说明没读到，它有值而 `battery_percent` 为 `None` 说明读到了但数值
  不可能。

  **阈值以下的合法低电量读数不受影响**：339 仍是 0%，340 仍是 5%。

* **持久化写入（`persist=True`）现在会在 `save` 之后等待 flash 写完再返回**，
  新增 `DEFAULT_SAVE_DELAY`（0.5 秒）与 `RegisterAccess.save_delay`。

  真机实测：设备写 flash 约需 300 ms，期间对部分寄存器回读出中间状态。写事务的
  文档说它是原子的，而在设备仍在忙时就返回，这个承诺不成立。等待在事务锁内，
  并发的读也因此不会挤进这个窗口。

  `persist=False` 不写 flash，不受影响——重连后的配置重放走的正是那条路径。

## 0.1.0

首个版本。WT9011DCL-BT50 的 BLE 接口：设备发现与连接、`0x55` 协议解析、寄存器
读写事务、校准、遥测、多设备并发采集。

### 许可证

MIT。

### 公开 API

* `scan` / `DiscoveredDevice` —— 设备发现
* `WT901Device` —— 生命周期、采样流、背压、自动重连
* `RegisterAccess`（`device.registers`）—— 寄存器读写事务与输出配置
* `Telemetry`（`device.telemetry`）—— 磁场、四元数、温度、电量、芯片时间、序列号、版本号
* `Calibration`（`device.calibration`）—— 加计校准与磁场校准
* `merge` —— 多设备样本流合并
* `wt901.protocol` —— 纯函数协议层（帧解码、指令构造、SI 换算），零 I/O

### 单位约定

对外一律 SI：加速度 m/s²、角速度 rad/s、角度 rad、磁场 µT、温度 °C、位移 m。
**不提供以「度」为单位的并行接口**——器件原始语义是度，转换只在一处发生，需要
原始计数值时走 `.raw`。

### 有意不做的事

* **时钟同步补偿**：`t_host` 是主机接收时刻，含蓝牙栈抖动；补偿是上层职责。
* **位移模式（`0x96`）**：位移帧与实时数据帧共用 Flag `0x61` 且布局无法区分，
  本库拒绝按运动语义解析而不是猜。
* **串口透传**：需要额外适配器硬件，目前无该硬件，不预留看似可用的接口。
* **速率编码 `0x0A`**：通用表标 125 Hz，真机实测 99.29 Hz，测不准的不进枚举。

### 不属于公开契约的部分

`wt901.recording` 与 `wt901.transport.replay` 是**测试设施**，用于在没有硬件的
环境里回放真机字节流。接口不承诺稳定。
